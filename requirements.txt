# Dependencias del Sistema de Gestión de Inventarios
# ==================================================

# Dependencias básicas (requeridas)
# El proyecto utiliza principalmente librerías estándar de Python

# Dependencias de seguridad (recomendadas)
cryptography>=3.4.8
bcrypt>=3.2.0
python-dotenv>=0.19.0

# Dependencias para desarrollo y testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-security>=0.0.1
bandit>=1.7.0
safety>=2.0.0

# Dependencias para calidad de código
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0
isort>=5.10.0

# Dependencias para análisis de seguridad
semgrep>=1.0.0
snyk>=1.0.0

# Dependencias para logging y monitoreo
structlog>=22.0.0
sentry-sdk>=1.0.0

# Dependencias para análisis de calidad de código
sonar-scanner>=4.0.0

# Dependencias para validación de entrada
cerberus>=1.3.4
marshmallow>=3.17.0

# Dependencias para manejo seguro de archivos
pathvalidate>=2.5.0
